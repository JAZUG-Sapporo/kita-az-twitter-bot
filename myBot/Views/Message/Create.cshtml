@model myBot.Models.Message
@{
    ViewBag.Title = _Localize.AddMessage;
    ViewBag.GoBackURL = Url.RouteUrl("Default", new { controller = "Bot", action = "Details", id = Model.BotID });
    ViewBag.GoBackText = _Localize.Back;
}

@Html.Partial("_MessageForm", Model)

@section Scripts {
    @Scripts.Render("~/Scripts/jqueryval")
}
